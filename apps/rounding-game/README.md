# Rounding Rally

A bright, touch-friendly game for practising visual estimation and rounding. Players build an approximate team before revealing the mystery quantity.

Run it from the repository root with `npm run dev -- rounding-game`.
